"""
video_utils — вспомогательные утилиты для видео.

⚠️ Reels (короткие видео) отключены по решению: в ежедневном контенте мы их не
используем. Функции генерации Reels удалены, чтобы не тянуть moviepy впустую.
Если Reels понадобятся снова — вернуть генератор Ken Burns из истории git.
"""

import logging

logger = logging.getLogger(__name__)
