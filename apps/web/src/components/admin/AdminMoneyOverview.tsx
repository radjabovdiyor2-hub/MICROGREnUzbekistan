'use client';

import React from 'react';
import { AdminStats } from './AdminStats';
import { AdminRevenue } from './AdminRevenue';

// ══════════════════════════════════════════════════════════════════════
// Сводка: сколько заработали — сегодня и за период, на одном экране.
//
// ЗАЧЕМ. «Сводка» и «Доход» были двумя пунктами меню, читали ОДИН И ТОТ
// ЖЕ раздел аналитики и отвечали на один вопрос — сколько денег пришло.
// Разница была только в горизонте: сутки против недели и месяца.
//
// Два пункта на один вопрос заставляли переключаться туда-обратно, чтобы
// понять, плохой сегодня день или плохая неделя, — а это ровно то
// сравнение, ради которого на цифры и смотрят.
//
// Сверху сегодня, ниже период: утренний взгляд сначала упирается в
// сутки, а неделя и месяц догоняют прокруткой, когда они понадобились.
// ══════════════════════════════════════════════════════════════════════

export function AdminMoneyOverview() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-4)' }}>
      <AdminStats />
      <AdminRevenue />
    </div>
  );
}
