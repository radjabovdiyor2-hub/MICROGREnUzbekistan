// ══════════════════════════════════════════════════════════════════════
// Правила кассы, общие для сервера и браузера.
//
// Здесь только константы: модуль подключается из клиентского компонента, и
// любая серверная зависимость утащила бы в браузерный бандл всё за собой.
// Раньше `SELLER_BACKDATE_DAYS` жил в `saleInput.ts`, а тот поднимает
// `adminAuth` (node:crypto) и `salesLedger` (Prisma) — календарь кассы
// импортировал константу и тянул за ней половину сервера.
// ══════════════════════════════════════════════════════════════════════

/**
 * Насколько глубоко в прошлое пускаем продавца.
 *
 * Забытая вчерашняя продажа — обычное дело, забытая позапрошломесячная —
 * уже переписывание закрытого периода. Владельцу предела нет: он и так
 * вправе править отчёты, и именно к нему идут с давними случаями.
 */
export const SELLER_BACKDATE_DAYS = 7;
